# --- Agent orchestration -------------------------------------------------
langgraph>=0.2.60
langchain-core>=0.3.29

# --- LLM provider (Groq: free tier, very fast inference) ------------------
langchain-groq>=0.2.3

# --- Local knowledge base ------------------------------------------------
langchain-chroma>=0.2.0
chromadb>=0.5.23

# --- Embeddings (local ONNX, no API key needed) --------------------------
langchain-community>=0.3.14
langchain-text-splitters>=0.3.5
fastembed>=0.5.0

# --- Web search fallback (Phase 3) ---------------------------------------
# Default provider: koi API key nahi chahiye
ddgs>=9.0.0
# Optional upgrade: behtar snippets, par signup chahiye (SEARCH_PROVIDER=tavily)
tavily-python>=0.5.0

# --- API layer (Phase 5) -------------------------------------------------
fastapi>=0.115.6
uvicorn[standard]>=0.34.0

# --- Config / validation -------------------------------------------------
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
