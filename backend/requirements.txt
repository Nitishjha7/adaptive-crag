# --- Agent orchestration -------------------------------------------------
langgraph>=0.2.60
langchain-core>=0.3.29

# --- LLM provider (Groq: free tier, very fast inference) ------------------
langchain-groq>=0.2.3

# --- Local knowledge base ------------------------------------------------
langchain-chroma>=0.2.0
chromadb>=0.5.23

# --- Embeddings (local ONNX, no API key needed) --------------------------
langchain-community>=0.3.14
langchain-text-splitters>=0.3.5
fastembed>=0.5.0

# --- Hybrid retrieval + reranking (Phase 9) ------------------------------
# BM25: keyword-based scoring. Vector search identifiers aur exact tokens pe
# kamzor hai; BM25 wahan strong hai. Dono ko RRF se fuse karte hain.
rank-bm25>=0.2.2
# Reranker cross-encoder fastembed me hi aata hai (TextCrossEncoder) — koi
# nayi dependency nahi, aur wahi local / ONNX / no-key rehta hai.

# --- Web search fallback (Phase 3) ---------------------------------------
# Default provider: koi API key nahi chahiye
ddgs>=9.0.0
# Optional upgrade: behtar snippets, par signup chahiye (SEARCH_PROVIDER=tavily)
tavily-python>=0.5.0

# --- API layer (Phase 5) -------------------------------------------------
fastapi>=0.115.6
uvicorn[standard]>=0.34.0

# --- Tests ---------------------------------------------------------------
pytest>=8.3.0
httpx>=0.28.0        # fastapi TestClient ke liye

# --- Config / validation -------------------------------------------------
pydantic>=2.10.4
pydantic-settings>=2.7.0
python-dotenv>=1.0.1
